/* pages/profile/profile.wxss - 我的页面样式（温馨可爱扁平插画风，与首页同款） */
.page {
  padding: 0 0 60rpx;
}
/* 返回主页按钮：白色药丸 + 暖色柔和阴影 */
.back-home {
  display: inline-flex;
  align-items: center;
  margin: 24rpx 24rpx 0;
  padding: 12rpx 30rpx;
  background: #ffffff;
  border-radius: 40rpx;
  color: #ffa94d;
  font-size: 26rpx;
  font-weight: 600;
  box-shadow: 0 6rpx 16rpx rgba(255, 169, 77, 0.2);
}

.back-home-icon {
  margin-right: 8rpx;
  font-size: 28rpx;
}

.back-home.hover {
  opacity: 0.8;
}

/* 用户头部：渐变背景 + 28rpx 圆角 + 柔和阴影 */
.user-card {
  display: flex;
  align-items: center;
  margin: 24rpx 24rpx 0;
  padding: 48rpx 36rpx;
  border-radius: 28rpx;
  background: linear-gradient(135deg, #ffc06b, #ffa94d);
  box-shadow: 0 12rpx 32rpx rgba(255, 169, 77, 0.4);
}

.avatar-btn {
  background: transparent;
  padding: 0;
  margin: 0;
  width: 140rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  border: none;
  line-height: 1.2;
}

.avatar-btn::after {
  border: none;
}

.avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: #ffffff;
  border: 4rpx solid #ffffff;
  box-shadow: 0 6rpx 16rpx rgba(44, 62, 80, 0.18);
}

.avatar-tip {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.9);
  margin-top: 12rpx;
}

.user-info {
  flex: 1;
  margin-left: 36rpx;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

.nickname-input {
  width: 100%;
  height: 76rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #ffffff;
  background: rgba(255, 255, 255, 0.32);
  border-radius: 16rpx;
  padding: 0 20rpx;
  box-sizing: border-box;
  margin-bottom: 20rpx;
}

/* 保存资料：药丸圆角 cute-btn 样式 */
.save-btn {
  width: 240rpx;
  padding: 14rpx 0;
  font-size: 26rpx;
  border-radius: 50rpx;
  background: #ffffff;
  color: #ffa94d;
  box-shadow: 0 6rpx 16rpx rgba(255, 169, 77, 0.25);
}

/* 区块卡片：使用 cute-card（大圆角 + 柔和阴影 + 四周留白） */
.section-card {
  margin: 0;
}

.section-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #2c3e50;
  padding-top: 8rpx;
  margin-bottom: 8rpx;
}

/* 空状态：放大图标、增加上下间距 */
.section-card .empty {
  padding: 140rpx 0;
}

.section-card .empty-icon {
  font-size: 110rpx;
  margin-bottom: 24rpx;
}

/* 上报/评价条目 */
.report-item {
  padding: 20rpx 0;
  border-bottom: 2rpx solid #f7ead6;
}

.report-item:last-child {
  border-bottom: none;
}

.report-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.report-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #2c3e50;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  margin-right: 20rpx;
}

.report-status {
  flex-shrink: 0;
  font-size: 22rpx;
  color: #e8833a;
  background: rgba(255, 169, 77, 0.16);
  border-radius: 24rpx;
  padding: 4rpx 14rpx;
}

.report-meta {
  font-size: 22rpx;
  color: #7f8c8d;
  margin-top: 8rpx;
}

.report-content {
  font-size: 26rpx;
  color: #5b6b79;
  margin-top: 10rpx;
}

/* 开发工具 */
.dev-tip {
  font-size: 24rpx;
  color: #7f8c8d;
  margin-bottom: 20rpx;
}

.dev-btn {
  width: 100%;
  padding: 14rpx 0;
  font-size: 28rpx;
  border-radius: 50rpx;
}