/* components/star/star.wxss */
.star-wrap {
  display: inline-flex;
  align-items: center;
  line-height: 1;
}
.star {
  margin-right: 4rpx;
  transition: transform 0.22s cubic-bezier(0.22, 1, 0.36, 1);
}
.star.on {
  transform: scale(1.05);
}