<!-- components/star/star.wxml -->
<view class="star-wrap" style="font-size: {{size}}rpx;">
  <text
    wx:for="{{[1,2,3,4,5]}}"
    wx:key="*this"
    class="star {{item <= tempValue ? 'on' : ''}}"
    data-index="{{item}}"
    bindtap="onTapStar"
  >★</text>
</view>
